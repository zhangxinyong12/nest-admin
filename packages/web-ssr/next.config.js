/** @type {import('next').NextConfig} */
const nextConfig = {
  pages: {
    // pages folder is relative to the project root (where the package.json file lives)
    src: "/app/pages",
  },
}

module.exports = nextConfig
